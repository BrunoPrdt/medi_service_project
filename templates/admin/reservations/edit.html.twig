{% extends 'base.html.twig' %}

{% block title %}Editer une réservation{% endblock %}

{% block body %}

    <section class="vert text-white pt-3">

        <div class="container-fluid">

            <h1 class="my-3">Editer une réservation</h1>

            {{ include('admin/reservations/_form.html.twig', {'button_label': 'Valider l\'édition'}) }}

            <div class="row">
                <div class="col-6 mt-3">
                    {{ include('admin/reservations/_delete_form.html.twig') }}
                </div>
                <div class="col-6 mt-3">
                    <a class="btn btn-secondary" href="{{ path('reservations_index') }}">Retour à la liste</a>
                </div>
            </div>
        </div>

    </section>

{% endblock %}
