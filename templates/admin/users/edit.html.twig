{% extends 'base.html.twig' %}

{% block title %}Editer un utilisateur{% endblock %}

{% block body %}

    <section class="vert text-white pt-3">

        <div class="container-fluid">

            <h1 class="my-5">Edition un utilisateur</h1>

            {{ include('admin/users/_form.html.twig', {'button_label': 'Valider l\'édition'}) }}

            <div class="row">
                <div class="col-6 mt-5">
                    {{ include('admin/users/_delete_form.html.twig') }}
                </div>
                <div class="col-6 mt-5">
                    <a class="btn btn-secondary" href="{{ path('users_index') }}">Retour à la liste</a>
                </div>
            </div>
        </div>

    </section>

{% endblock %}
