{{ form_start(form) }}
    <div class="row">
        <div class="form-group col-md-4">
            {{ form_row(form.username, {'label' : 'Nom d\'utilisateur', 'attr':{'placeholder': 'Nom...'}}) }}
        </div>
        <div class="form-group col-md-4">
            {{ form_row(form.mail, {'label' : 'Email', 'attr':{'placeholder' : 'Email...'}}) }}
        </div>
        <div class="form-group col-md-4">
            {{ form_row(form.category, {'label' : 'Poste', 'attr' : {'placeholder' : 'Commercial, Responsable, ...'}}) }}
        </div>
    </div>

    <div class="row">
        <div class="form-group col-md-6">
            {{ form_row(form.password, {'label' : 'Mot de passe', 'attr' : {'placeholder' : 'Mot de passe ...'}}) }}
        </div>
            <div class="form-group col-md-6">
                {{ form_row(form.confirm_password, {'label' : 'Confirmation mot de passe', 'attr' : {'placeholder' : 'Confirmez votre mot de passe ...'}}) }}
        </div>
    </div>
    <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
