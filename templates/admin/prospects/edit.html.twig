{% extends 'base.html.twig' %}

{% block title %}Edition d'un prospect{% endblock %}

{% block body %}

    <section class="vert text-white pt-3">

        <div class="container-fluid">

            <h1 class="my-3">Edition d'un prospect</h1>

            {{ include('admin/prospects/_form.html.twig', {'button_label': 'Valider l\'édition'}) }}

            <div class="row">
                <div class="col-6 mt-3">
                    {{ include('admin/prospects/_delete_form.html.twig') }}
                </div>
                <div class="col-6 mt-3">
                    <a class="btn btn-secondary" href="{{ path('prospects_index') }}">Retour à la liste</a>
                </div>
            </div>
        </div>

    </section>

{% endblock %}
